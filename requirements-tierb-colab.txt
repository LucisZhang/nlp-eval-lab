# Pinned Tier-B training deps for the cloud GPU box (Colab / rented T4/A10).
# Versions match the repo lockfile's `tierb` optional group so the checkpoints you
# train remotely load byte-consistently under the local eval harness.
#
# torch is intentionally NOT pinned here: Colab ships a CUDA-matched torch build and
# you must use it (a pip-installed CPU wheel would ignore the GPU). transformers 5.x
# requires torch>=2.4, which Colab already satisfies. If your box has no torch, install
# the CUDA build from https://pytorch.org first, then this file.
transformers==5.14.1
accelerate==1.14.0
tokenizers==0.22.2
safetensors==0.8.0
pyarrow==25.0.0
pyyaml>=6.0
numpy>=2.0
