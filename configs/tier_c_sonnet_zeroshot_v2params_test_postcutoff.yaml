# v2-params PROBE (owner-approved 2026-08-07): identical to
# tier_c_sonnet_zeroshot_test_postcutoff except max_tokens 64 -> 256, to quantify how much
# the 64-token completion cap (37/1,500 length-truncated calls -> fallback labels) understates
# Sonnet 5 on the drifted slice. Same frozen v1 prompt bundle, same 1,500 shared rows
# (cap_seed 20260806). The v1 run (run_id d1c42d7d...) stays PRIMARY; this is a probe.
model:
  runner: tier_c
  name: tier_c_sonnet_zeroshot_v2params_test_postcutoff
  slug: anthropic/claude-sonnet-5
  params:
    temperature: 0.0
    max_tokens: 256
    request_timeout_s: 60
    max_retries: 3
    max_concurrency: 8
prompt:
  version: v1
  num_exemplars: 0
data:
  split: test_postcutoff
  eval_rows_cap: 1500
  cap_seed: 20260806
  text_column: narrative
  label_column: class
  order_column: complaint_id
seed: 20260806
